using UnityEngine;

public class BuildableTower : MonoBehaviour
{
    public Sprite icon;
    public int cost = 50;

    public int damage = 50;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
